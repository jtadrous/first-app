import React from 'react';

const ProductsPage = () => {
    return (
        <React.Fragment>
            <h1>Products Page</h1>
        </React.Fragment>
    );
};

export default ProductsPage;

//the link to this page would be http://localhost:8000/products bc it is the index page of the folder